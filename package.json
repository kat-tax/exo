{
  "private": true,
  "scripts": {
    "start": "pnpm run build && conc pnpm:*-dev",
    "build": "conc -g pnpm:*-build",

    "config-dev": "pnpm --filter ./.config dev",
    "config-build": "pnpm --filter ./.config build",

    "translate-dev": "conc -P 'pnpm:translate-extract {1}' 'pnpm:translate-compile {1}' -- --watch",
    "translate-build": "pnpm run translate-extract && pnpm run translate-compile",
    "translate-extract": "lingui extract --config ./client/lingui.config.ts",
    "translate-compile": "lingui compile --config ./client/lingui.config.ts",

    "design-xdev": "pnpm --filter ./design dev",
    "design-xbuild": "pnpm --filter ./design build",
    "design-publish": "pnpm --filter ./design publish",

    "library-dev": "pnpm --filter ./library dev",
    "library-build": "pnpm --filter ./library build",
    "library-publish": "pnpm --filter ./library publish",

    "docs-dev": "pnpm --filter ./toolkit/apps/docs dev",
    "docs-build": "pnpm --filter ./toolkit/apps/docs build",

    "storybook-dev": "conc pnpm:storybook-web pnpm:storybook-native",
    "storybook-build": "pnpm --filter ./toolkit/apps/storybook/web run build",
    "storybook-native": "pnpm --filter ./toolkit/apps/storybook/native run dev",
    "storybook-web": "pnpm --filter ./toolkit/apps/storybook/web run dev",

    "web-dev": "pnpm --filter ./client run dev",
    "web-build": "pnpm --filter ./client run build",

    "postinstall": "pnpm run build"
  },
  "devDependencies": {
    "@lingui/cli": "latest",
    "concurrently": "latest"
  },
  "resolutions": {
    "@swc/core": "1.3.56"
  }
}
